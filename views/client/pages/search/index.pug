extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout-grid.pug
block main 
    .container 
        .row 
            .col-12
                +box-head("Result")
        if(products.length>0)
            +product-layout-grid(products)
        else 
            h3(class="text-danger") Cannot find any product 🥲